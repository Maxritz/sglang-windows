#pragma once
// Redirect: hipify-gen'd duplicate of ATen/cuda/CUDAContext.h (CUDA-named
// content). Pull the cuda twin so the ATen CUDA context helpers are declared once.
#include <ATen/cuda/CUDAContext.h>
